import React from "react";
import { HeaderContainer } from "./styles";

const Header: React.FC = () => {
    return(
        <HeaderContainer>
            Calendar
        </ HeaderContainer>
    )
};

export default Header;
